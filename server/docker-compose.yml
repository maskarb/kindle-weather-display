version: '3'

services:
  kindle-server:
    image: kindle-server
    environment:
        - CLIMACELL_API_KEY=${CLIMACELL_API_KEY}
        - LATITUDE=${LATITUDE}
        - LONGITUDE=${LONGITUDE}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 53084:53084
